programa patito:
renglon entero -> arr[2];
variable entero -> j, x;

funcion sinregresar encontrarArreglo(){
  arr[0] = 2;
  arr[1] = 1;

  desde(j = 0) hasta (2) {
    si (arr[j] == 2) {
      imprimir(arr[j]);
    };
  };
};


inicio () {
  encontrarArreglo();
};