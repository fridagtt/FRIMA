programa pruebaUno:
variable entero -> gatos, perros, alumno;
variable decimal -> pluma, pelos, lapiz;

funcion sinregresar funcion_prueba(entero hojas, decimal flores){
   variable entero -> pluma, pelos;
   lapiz = lapiz*(pluma-pelos);

};

funcion sinregresar funcion_prueba_tres(letra paloma){
   variable entero -> pluma, pelos;
   lapiz = lapiz*(pluma-pelos);
};

inicio(){
  funcion_prueba(gatos*perros,pluma+lapiz);
};