programa frima:

variable decimal -> a,b,c,d,e,f,g,h,j,k;

funcion sinregresar uno() { 

  a = b + c * (d - e / f) * h;
  b = e - f;

  mientras ( a * b - c >= d * e / (g + h)) {
    h = j * k + b;
    si (b < h) {
      b = h + j;
      mientras ( b > a + c) {
        imprimir ( a + b * c, d - e);
        b = b - j;
      };
    }
    sino {
      a = a + b;
      imprimir (b - d);
    };
  };
};

inicio () {
	
};