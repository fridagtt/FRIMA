programa frima:

variable entero -> a,b,c,d,e,f,g,h,j,k;

funcion sinregresar imprime (){
  imprimir ("hola");
};

funcion entero uno(entero n) { 

  desde (a = 4) hasta (n+5*6) {
    imprimir (a);
    imprime();
  };

  regresar (a);
};

inicio () {

 
	imprimir(uno(10));

};