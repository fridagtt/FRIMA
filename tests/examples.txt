programa pruebaUno:
variable entero -> gatos, perros, alumno;
variable decimal -> pluma, pelos, lapiz;

funcion entero funcion_prueba(entero hojas, decimal flores){
   variable entero -> pluma, pelos;

   imprimir(2, "string");
   lapiz = lapiz*(pluma-pelos);

   regresar lapiz;
};

// This is a single comment

/*
funcion entero funcion_prueba_dos(letra paloma){
   variable entero -> pluma, pelos;

   imprimir(2, "string");
   lapiz = lapiz*(pluma-pelos);

   regresar lapiz;
};
*/

funcion entero funcion_prueba_tres(letra paloma){
   variable entero -> pluma, pelos;

   imprimir(2, "string");
   lapiz = lapiz*(pluma-pelos);

   regresar lapiz;
};

inicio(){
   lapiz = pluma * perros;
};