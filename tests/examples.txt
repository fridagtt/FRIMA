programa pruebaUno:
variable entero -> gatos, perros, alumno;
variable decimal -> pluma, pelos, lapiz;

funcion entero funcion_prueba(){
   variable entero -> pluma, pelos;

   imprimir(2, "string");
   lapiz = lapiz*(pluma-pelos);

   regresar lapiz;
};

// This is a single comment

/*
funcion entero funcion_prueba_dos(){
   variable entero -> pluma, pelos;

   imprimir(2, "string");
   lapiz = lapiz*(pluma-pelos);

   regresar lapiz;
};
*/

funcion entero funcion_prueba_tres(){
   variable entero -> pluma, pelos;

   imprimir(2, "string");
   lapiz = lapiz*(pluma-pelos);

   regresar lapiz;
};

inicio(){
   lapiz = pluma * perros;
};